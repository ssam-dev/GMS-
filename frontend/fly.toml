app = "gms-frontend"
primary_region = "iad"

[build]
builder = "docker"
dockerfile = "Dockerfile"

[services]
internal_port = 3000
protocol = "tcp"

[[services.ports]]
port = 80
handlers = ["http"]

[[services.ports]]
port = 443
handlers = ["tls", "http"]
